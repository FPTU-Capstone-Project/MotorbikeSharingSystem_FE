# Production Environment Variables for Vercel
# ⚠️ Using Vercel Proxy to avoid MIXED CONTENT error (HTTPS → HTTP)
# Vercel will proxy /api/* requests to EC2 backend
# See vercel.json for proxy configuration

# Use relative path - Vercel will proxy to http://13.215.143.121/api/v1
REACT_APP_API_BASE_URL=/api/v1

# Original backend URL (for reference)
# REACT_APP_API_BASE_URL=http://13.215.143.121/api/v1

GENERATE_SOURCEMAP=false
